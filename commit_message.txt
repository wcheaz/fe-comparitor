Ralph iteration 1: Implement PromotionPathPlanner component with multi-tier selection UI

Tasks completed:
- [x] 1.1 Create file `components/features/PromotionPathPlanner.tsx` with basic component structure
- [x] 1.2 Import React, `useState`, `useMemo` from 'react'
- [x] 1.3 Import UI primitives: `Select`, `SelectContent`, `SelectItem`, `SelectTrigger`, `SelectValue` from `@/components/ui/select`
- [x] 1.4 Import `Button` from `@/components/ui/button`
- [x] 1.5 Import `Unit`, `GameData`, `PromotionEvent` interfaces from `@/types` or relevant data lib
- [x] 1.6 Import icons: `PlusIcon`, `MinusIcon` from `lucide-react`
- [x] 1.7 Define interface `PromotionPathPlannerProps` with properties: `unit` (Unit | null), `promotionEvents` (PromotionEvent[]), and `onPromotionChange` ((events: PromotionEvent[]) => void)
- [x] 1.8 Create and export the `PromotionPathPlanner` functional component using the defined props
- [x] 1.9 Add an early return: `if (!unit) return null;`
- [x] 1.10 Return a basic `<div className=\"space-y-4\">` container wrapping a header `<h3 className=\"text-lg font-semibold\">Promotion Path for {unit.name}</h3>`
- [x] 2.1 In `PromotionPathPlanner.tsx`, create a `useMemo` hook named `promotionTiers` returning an array of valid class options per tier
- [x] 2.2 Inside `promotionTiers`, initialize a `tiers` array of arrays: `let tiers: any[][] = [];`
- [x] 2.3 Inside `promotionTiers`, define the starting class ID: `let currentClassId = unit.classId;`
- [x] 2.4 Inside `promotionTiers`, loop through `promotionEvents.length + 1` times. At each step, find the current class in `GameData.classes` matching `c.game === unit.game && c.id === currentClassId`
- [x] 2.5 Inside the loop, if the class has a `promotesTo` array (length > 0), map these IDs to comprehensive class objects and push to `tiers`
- [x] 2.6 Inside the loop, update `currentClassId` to `promotionEvents[step]?.classId` for the next iteration (verifying the user's selected path)
- [x] 3.1 In `PromotionPathPlanner.tsx`, beneath the header, map over the `promotionTiers` array
- [x] 3.2 For each tier, render a flex container with a "Tier {index + 1}" label and a `<Select>` dropdown
- [x] 3.3 Set the `<Select>` value to `promotionEvents[index]?.classId || ''`
- [x] 3.4 Populate `<SelectItem>` components inside the dropdown using the class objects resolved in the current `tier` array
- [x] 4.1 In `PromotionPathPlanner.tsx`, create `handleClassSelect(index: number, newClassId: string)`
- [x] 4.2 Inside `handleClassSelect`, clone `promotionEvents`
- [x] 4.3 Still in `handleClassSelect`, update the target index: `newEvents[index] = { ...newEvents[index], classId: newClassId }`
- [x] 4.4 Set default level if creating a new event: `if (!newEvents[index].level) newEvents[index].level = 10;`
- [x] 4.5 Slice off invalid higher tiers (e.g. `newEvents = newEvents.slice(0, index + 1);`)
- [x] 4.6 Call `onPromotionChange(newEvents)`
- [x] 4.7 Wire `handleClassSelect` to the `<Select>` `onValueChange` prop
- [x] 5.1 In `PromotionPathPlanner.tsx`, structure a `div` at the bottom for "+" and "-" `<Button>`s
- [x] 5.2 Add `onClick` to "+": Append a new `PromotionEvent` containing the first `promotesTo` class from the final tier, and call `onPromotionChange`
- [x] 5.3 Add `disabled` state to "+": Disable if the latest class in the chain has `promotesTo.length === 0`
- [x] 5.4 Add `onClick` to "-": Call `onPromotionChange(promotionEvents.slice(0, -1))`
- [x] 5.5 Add `disabled` state to "-": Disable if `promotionEvents.length === 0`
- [x] 6.1 Open `app/comparator/page.tsx`. Import `PromotionPathPlanner`
- [x] 6.2 In `page.tsx`, directly below the `<ComparisonGrid>`, add a new two-column CSS grid (`grid-cols-2 gap-8`)
- [x] 6.3 In the left column, render `<PromotionPathPlanner unit={unit1} promotionEvents={unit1Promotions} onPromotionChange={setUnit1Promotions} />`
- [x] 6.4 In the right column, render `<PromotionPathPlanner unit={unit2} promotionEvents={unit2Promotions} onPromotionChange={setUnit2Promotions} />`
- [x] 6.5 Open `components/features/ComparisonGrid.tsx`. Locate the "Promotion Options" `<TableRow>` (approx line 535) and fully delete it
- [x] 6.6 Start `npm run dev` and manually verify the new planner correctly drives changes in the `StatProgressionTable`